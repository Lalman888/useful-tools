node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Runtime data: uploaded files and their metadata never belong in git.
# The directory is created on startup, so it does not need to be tracked.
/data/

.env
.env.local
.DS_Store
npm-debug.log*
